% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paintr-placeholders.R
\name{ptr_resolve_layer_data}
\alias{ptr_resolve_layer_data}
\title{Resolve the Dataset for a ggpaintr Layer}
\usage{
ptr_resolve_layer_data(ptr_obj, layer_name, input, context, eval_env)
}
\arguments{
\item{ptr_obj}{A \code{ptr_obj} (available as \code{context$ptr_obj} inside a
\code{bind_ui()} callback).}

\item{layer_name}{Layer name as a string. Use \code{meta$layer_name} to scope
to the meta currently being bound, or \code{"ggplot"} for the base layer.}

\item{input}{A Shiny \code{input} reactive values object (the first argument
of \code{bind_ui()}).}

\item{context}{The placeholder context (the fourth argument of
\code{bind_ui()}).}

\item{eval_env}{An evaluation environment, typically \code{context$eval_env}.}
}
\value{
A named list with components \code{has_data} (logical scalar) and
\code{data} (the resolved data frame, or \code{NULL}).
}
\description{
Return the active data frame for a parsed layer (e.g. \code{"ggplot"},
\code{"geom_point"}). Designed to be called from a custom placeholder's
\code{bind_ui()} callback when the widget needs to know columns or values
from the dataset that the user is currently plotting.
}
\details{
Resolution order:
\enumerate{
\item Look up the layer's \code{data} argument by parameter name. If the layer
has a \verb{data = <placeholder>} argument (e.g. \code{data = upload}), evaluate
that placeholder to get the data frame.
\item If the \code{data} argument is an unbound symbol, evaluate it in \code{eval_env}.
\item Otherwise fall back to the first positional argument when it is a bare
symbol (e.g. \code{ggplot(mtcars, aes(...))}). This heuristic does \strong{not}
handle non-data-first call shapes such as \code{merge(x, y)}; in those
cases supply \verb{data = } explicitly in the formula.
}

Returns \code{list(has_data = FALSE, data = NULL)} if no dataset is resolvable,
so callers can early-return without rendering an empty widget.
}
\seealso{
\code{\link[=ptr_define_placeholder]{ptr_define_placeholder()}}, \code{\link[=ptr_ns_id]{ptr_ns_id()}}.
}
