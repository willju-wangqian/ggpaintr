% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paintr-utils.R
\name{ptr_ns_id}
\alias{ptr_ns_id}
\title{Apply a Namespace Function to a Placeholder Id}
\usage{
ptr_ns_id(ns_fn, id)
}
\arguments{
\item{ns_fn}{A namespace function with signature \code{character -> character},
typically \code{shiny::NS(id)} or \code{shiny::NS(NULL)}.}

\item{id}{A single id string (e.g., \code{meta$id} from a placeholder context).}
}
\value{
The namespaced id string.
}
\description{
Resolve the namespaced Shiny input/output id for a placeholder.
Custom \code{bind_ui} callbacks should call this with the namespace function
supplied on the placeholder context, never assume \code{meta$id} is already
namespaced.
}
\details{
Inside a custom \code{bind_ui(input, output, metas, context)} use:

\if{html}{\out{<div class="sourceCode">}}\preformatted{input_id  <- ptr_ns_id(context$ns_fn    \%||\% shiny::NS(NULL), meta$id)
output_id <- ptr_ns_id(context$ui_ns_fn \%||\% shiny::NS(NULL), meta$id)
}\if{html}{\out{</div>}}

Under \code{\link[=ptr_app]{ptr_app()}} both \code{ns_fn} and \code{ui_ns_fn} default to \code{shiny::NS(NULL)}
so the namespaced id equals \code{meta$id}. When ggpaintr is embedded inside a
Shiny module (any wrapper that supplies a real namespace), they wrap a
module namespace and the two ids diverge. Always go through \code{ptr_ns_id()}
rather than building the id string yourself.
}
\examples{
ns <- shiny::NS("mod1")
ptr_ns_id(ns, "ggplot_3_2")
ptr_ns_id(shiny::NS(NULL), "ggplot_3_2")
}
\seealso{
\code{\link[=ptr_define_placeholder]{ptr_define_placeholder()}}, \code{\link[=ptr_resolve_layer_data]{ptr_resolve_layer_data()}}.
}
