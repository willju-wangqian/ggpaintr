% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paintr-app.R
\name{ptr_server}
\alias{ptr_server}
\title{Register ggpaintr Server Logic}
\usage{
ptr_server(
  input,
  output,
  session,
  formula,
  envir = parent.frame(),
  ui_text = NULL,
  placeholders = NULL,
  ids = ptr_build_ids(),
  checkbox_defaults = NULL,
  expr_check = TRUE,
  safe_to_remove = character(),
  shared = list(),
  draw_trigger = NULL,
  ns = shiny::NS(NULL)
)
}
\arguments{
\item{input}{A Shiny \code{input} object.}

\item{output}{A Shiny \code{output} object.}

\item{session}{A Shiny \code{session} object.}

\item{formula}{A single formula string using \code{ggpaintr} placeholders.}

\item{envir}{Environment used to resolve local data objects when building the app.}

\item{ui_text}{Optional named list of copy overrides for UI labels, helper
text, and placeholders.}

\item{placeholders}{Optional custom placeholder definitions or an existing
placeholder registry.}

\item{ids}{A \code{ptr_build_ids} object controlling the Shiny element IDs used by
the integration helpers. Defaults to \code{ptr_build_ids()}.}

\item{checkbox_defaults}{Optional named list of initial checked states for
layer checkboxes. See \code{\link[=ptr_server_state]{ptr_server_state()}}.}

\item{expr_check}{Controls \code{expr} placeholder validation.
\code{TRUE} (default) applies the built-in denylist of dangerous
functions.
\code{FALSE} disables all checking.
A named list with \code{deny_list} and/or \code{allow_list} character
vectors supplies a custom check; when both are given,
denied entries are removed from the allowlist.}

\item{safe_to_remove}{Character vector of additional function names whose
zero-argument calls should be dropped after placeholder substitution
leaves them empty. Extends the curated default set: \code{theme()}, \code{labs()},
\code{xlab()}, \code{ylab()}, \code{ggtitle()}, \code{facet_wrap()}, \code{facet_grid()},
\code{facet_null()}, \code{xlim()}, \code{ylim()}, \code{lims()}, \code{expand_limits()},
\code{guides()}, \code{annotate()}, \code{annotation_custom()}, \code{annotation_map()},
\code{annotation_raster()}, \code{aes()}, \code{aes_()}, \code{aes_q()}, \code{aes_string()},
\code{vars()}, \code{element_text()}, \code{element_line()}, \code{element_rect()},
\code{element_point()}, \code{element_polygon()}, \code{element_geom()}. \verb{geom_*()} /
\verb{stat_*()} standalone layers are always preserved. Defaults to \code{character()}.}

\item{shared}{Named list of Shiny reactives (or \code{NULL}/empty list).
See \code{\link[=ptr_server_state]{ptr_server_state()}} for details. Use this to drive multiple
ptr_obj instances from a single externally-rendered control.}

\item{draw_trigger}{Optional Shiny reactive (or \code{NULL}). See
\code{\link[=ptr_server_state]{ptr_server_state()}} for details.}

\item{ns}{An optional namespace function (\code{character -> character}). See
\code{\link[=ptr_server_state]{ptr_server_state()}} for details.}
}
\value{
A \code{ptr_state} object containing reactive accessors named \code{obj},
\code{runtime}, and \code{var_ui_list}, plus shared metadata used by the bind helpers.
}
\description{
Wire the standard \code{ggpaintr} server behavior into an existing Shiny app.
The returned state object exposes reactive access to the parsed formula,
latest runtime result, and current dynamic \code{var} UI definitions so callers
can extend the app with additional observers and outputs.
}
\note{
For Shiny modules, prefer \code{\link[=ptr_module_ui]{ptr_module_ui()}} and
\code{\link[=ptr_module_server]{ptr_module_server()}}. \code{moduleServer()} already scopes \code{input} and
\code{output}, while UI generated from \code{renderUI()} still needs \code{session$ns};
the module wrappers handle that split.
}
