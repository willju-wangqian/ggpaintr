% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paintr-app.R
\name{ptr_gg_extra}
\alias{ptr_gg_extra}
\title{Capture Out-of-Runtime ggplot Additions for the Code Output}
\usage{
ptr_gg_extra(ptr_state, ...)
}
\arguments{
\item{ptr_state}{A \code{ptr_state} object created by \code{\link[=ptr_server_state]{ptr_server_state()}}.}

\item{...}{ggplot components (theme, scale, coord, guides, labs, ...).
Captured with \code{\link[rlang:enquo]{rlang::enquos()}} for code output, evaluated for the
returned list.}
}
\value{
A list of evaluated ggplot components, suitable for
\code{plot_obj + ptr_gg_extra(ptr_state, ...)}.
}
\description{
Advanced helper for custom \code{renderPlot({...})} blocks inside embedded
ggpaintr apps. When an advanced developer adds ggplot components (themes,
scales, coords, guides, labs, ...) to the plot returned by
\code{\link[=ptr_extract_plot]{ptr_extract_plot()}}, those additions render visually but never reach the
generated code output. \code{ptr_gg_extra()} solves that by capturing the
expressions it receives and storing them on the \code{ptr_state} so the default
code binder (\code{\link[=ptr_register_code]{ptr_register_code()}}) can surface them alongside the
formula-driven code.
}
\details{
The helper is intentionally \strong{not} exposed through \code{\link[=ptr_app]{ptr_app()}} or
\code{\link[=ptr_app_bslib]{ptr_app_bslib()}}. It is only meaningful when the caller owns their own
\code{renderPlot({...})} block built on \code{\link[=ptr_server_state]{ptr_server_state()}} and the
\verb{ptr_register_*()} helpers.

Semantics:
\itemize{
\item Replace-per-call. Each call overwrites the previously captured extras.
Pass every component you want in a single call, e.g.
\code{ptr_gg_extra(ps, theme_minimal(), scale_x_log10())}.
\item Suppression on failure. When the underlying runtime reports
\code{ok = FALSE}, the code binder ignores extras so stale values from a
prior successful draw never surface during a failed draw.
\item No new S3 class. The return value is a plain list, so
\code{plot_obj + ptr_gg_extra(ps, ...)} dispatches through ggplot2's built-in
\code{ggplot_add.list} method.
}
}
\examples{
if (interactive()) {
server <- function(input, output, session) {
  ps <- ptr_server_state(
    "ggplot(data = iris, aes(x = var, y = var)) + geom_point() + labs(title = text)"
  )
  ptr_setup_controls(input, output, ps)
  ptr_register_draw(input, ps)
  ptr_register_error(output, ps)
  ptr_register_code(output, ps)

  output$outputPlot <- shiny::renderPlot({
    plot_obj <- ptr_extract_plot(ps$runtime())
    if (is.null(plot_obj)) {
      graphics::plot.new()
      return(invisible(NULL))
    }
    plot_obj + ptr_gg_extra(ps, ggplot2::theme_minimal(base_size = 16))
  })
}
}
}
