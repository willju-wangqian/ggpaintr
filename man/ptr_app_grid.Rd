% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paintr-app.R
\name{ptr_app_grid}
\alias{ptr_app_grid}
\title{Multi-Plot Shiny App With Shared Placeholder Controls}
\usage{
ptr_app_grid(
  plots,
  shared_ui = list(),
  envir = parent.frame(),
  title = "ggpaintr grid",
  draw_all_label = "Draw all",
  expr_check = TRUE
)
}
\arguments{
\item{plots}{Character vector or list of formula strings; one ptr_obj
per entry.}

\item{shared_ui}{Named list of \verb{function(id) -> shiny.tag}. Each name
must match a \code{shared = "<id>"} annotation used by at least one of the
\code{plots} formulas. The function is called with the shared name as the
widget input id; the resulting widget is rendered once at the top of
the app and its \code{input[[id]]} is wired into every plot's
\code{shared} argument.}

\item{envir}{Environment used to resolve local data objects.}

\item{title}{Title displayed in the app header.}

\item{draw_all_label}{Label for the top-level "draw all" button that
triggers a redraw of every plot in the grid. The button is rendered
alongside the shared widgets so changes to those widgets can be
broadcast to all plots in one click. Defaults to \code{"Draw all"}.}

\item{expr_check}{Controls \code{expr} placeholder validation. See
\code{\link[=ptr_server_state]{ptr_server_state()}}.}
}
\value{
A \code{shiny.appobj}. Run with \code{shiny::runApp()} or print at the REPL.
}
\description{
Build a single Shiny app that hosts multiple ggpaintr formulas in one
session and drives them from a top region of shared controls. Each
formula becomes its own ptr_obj with its own draw button and plot output.
Placeholders carrying a \code{shared = "<id>"} annotation read their value
from the matching widget in \code{shared_ui}; non-shared placeholders still
render their per-instance widgets in each plot's panel as usual.
}
\details{
Layout is fixed: shared controls in a top \code{wellPanel}, then one
equally-sized column per plot below. For more flexible arrangements,
use \code{ptr_server()} directly inside your own custom Shiny UI.
}
\examples{
if (interactive()) {
  app <- ptr_app_grid(
    plots = list(
      'ggplot(data = mtcars, aes(x = wt, y = mpg)) +
         geom_point(size = num(shared = "sz"))',
      'ggplot(data = mtcars, aes(x = hp, y = mpg)) +
         geom_point(size = num(shared = "sz"))'
    ),
    shared_ui = list(
      sz = function(id) shiny::sliderInput(id, "Size", 1, 10, value = 3)
    )
  )
  shiny::runApp(app)
}
}
