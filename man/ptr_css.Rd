% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paintr-css-doc.R
\name{ptr_css}
\alias{ptr_css}
\title{CSS customisation hooks for ggpaintr apps}
\description{
Every raw-Shiny entry point (\code{\link[=ptr_app]{ptr_app()}}, \code{\link[=ptr_app_grid]{ptr_app_grid()}},
\code{\link[=ptr_ui]{ptr_ui()}}, \code{\link[=ptr_shared_panel]{ptr_shared_panel()}}) accepts a \verb{css =} argument: a character vector of
paths to \code{.css} files. Each path's parent directory is registered as a
Shiny resource under a hash-derived prefix, and one \verb{<link>} tag is
emitted per file. User stylesheets are linked \emph{after} \code{ggpaintr}'s
bundled stylesheet, so rules at equal specificity win.
}
\details{
The bundled stylesheet exposes its theme as CSS custom properties
under the \code{.ptr-app} selector. Override any subset of these in a
user-supplied stylesheet to retheme without touching individual rules.
}
\section{Tokens}{

\describe{
\item{\code{--ptr-bg}}{App background. Default \verb{#fafbfc}.}
\item{\code{--ptr-surface}}{Card / sidebar background. Default \verb{#ffffff}.}
\item{\code{--ptr-border}}{Standard border colour. Default \verb{#e7e9ee}.}
\item{\code{--ptr-ink}}{Body text colour. Default \verb{#1f2530}.}
\item{\code{--ptr-muted}}{Muted / secondary text. Default \verb{#6b7280}.}
\item{\code{--ptr-accent}}{Primary accent (action button bg). Default \verb{#c1372c}.}
\item{\code{--ptr-accent-strong}}{Hover / active accent. Default \verb{#9a2b22}.}
\item{\code{--ptr-accent-weak}}{Faint accent tint. Default \code{rgba(193,55,44,0.13)}.}
\item{\code{--ptr-accent-line}}{Accent border. Default \code{rgba(193,55,44,0.22)}.}
\item{\code{--ptr-radius}}{Large radius (cards, sidebar). Default \verb{10px}.}
\item{\code{--ptr-radius-sm}}{Small radius (buttons, controls). Default \verb{7px}.}
\item{\code{--ptr-shadow}}{Card shadow. Default two-layer drop.}
\item{\code{--ptr-shadow-lg}}{Floating-window shadow. Default two-layer drop.}
\item{\code{--ptr-font}}{UI font stack. Default system-ui stack.}
\item{\code{--ptr-mono}}{Mono font stack. Default system mono stack.}
}
}

\section{Specificity}{

Some rules use a doubly-scoped selector (e.g. \verb{.ptr-app .btn.action-button}).
To override these, match or exceed that specificity in your stylesheet
(either with the same selector, or with \code{!important}).
}

\examples{
\dontrun{
  # accent.css:
  #   .ptr-app { --ptr-accent: #2563eb; --ptr-accent-strong: #1e40af; }
  ptr_app("ggplot(mtcars, aes(ppVar, ppVar)) + geom_point()",
          css = "accent.css")
}

}
\keyword{internal}
