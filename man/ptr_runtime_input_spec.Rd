% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paintr-parse.R
\name{ptr_runtime_input_spec}
\alias{ptr_runtime_input_spec}
\title{Describe the Runtime Inputs for a Parsed Formula}
\usage{
ptr_runtime_input_spec(ptr_obj)
}
\arguments{
\item{ptr_obj}{A \code{ptr_obj}.}
}
\value{
A base \code{data.frame} with columns \code{input_id}, \code{role}, \code{layer_name},
\code{keyword}, \code{param_key}, \code{source_id}, and \code{shared}. The \code{shared} column
carries the user-supplied identifier from a call-form placeholder
(e.g., \code{num(shared = "year_filter")}); it is \code{NA_character_} for bare
placeholders and for non-placeholder rows.
}
\description{
Return the input ids consumed by \code{ptr_exec()} in a stable,
documented data frame. This is the supported low-level discovery helper for
tests, tooling, and advanced package authors who need to construct an input
list programmatically without hard-coding internal ids in documentation or
downstream code.
}
\details{
The returned rows follow the current UI/runtime order:
\itemize{
\item placeholder inputs in parsed layer order
\item derived upload dataset-name inputs immediately after each \code{upload}
placeholder row
\item layer checkbox inputs for every non-\code{ggplot} layer, appended in layer order
}

Raw ids such as \code{"ggplot_3_2"} still appear in the returned data, but those
ids remain implementation details. Call this helper instead of relying on the
exact encoding scheme directly.
}
\examples{
obj <- ptr_parse_formula(
  "ggplot(data = mtcars, aes(x = var, y = var)) + geom_point() + labs(title = text)"
)
ptr_runtime_input_spec(obj)
}
