% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paintr-app.R
\name{ptr_ui_code_toggle}
\alias{ptr_ui_code_toggle}
\title{Toggle-Wired Plot + Slide-Out Code Block}
\usage{
ptr_ui_code_toggle(id = NULL, plot_error = TRUE)
}
\arguments{
\item{id}{Optional module id; the namespace prefix for the outputs.
Defaults to \code{NULL} (identity namespace). When set, must match the
\code{id} passed to \code{\link[=ptr_ui_controls]{ptr_ui_controls()}} and the server wiring.}

\item{plot_error}{If \code{TRUE} (default) the inline error slot is nested
in the plot card body, matching the bundled layout. Set \code{FALSE} to
place \code{\link[=ptr_ui_error]{ptr_ui_error()}} yourself.}
}
\value{
A \link[shiny:reexports]{shiny::tag}.
}
\description{
Convenience helper that returns the plot card and the slide-out
generated-code window wrapped in the \code{.ptr-output} scope the bundled
JavaScript needs, with the show-code button wired to open/close and
drag the code window. This is exactly the block \code{\link[=ptr_app]{ptr_app()}} and
\code{\link[=ptr_module_ui]{ptr_module_ui()}} render internally.
}
\details{
Use this when you want the familiar toggle behaviour while still
owning the surrounding layout. For fully independent placement of the
plot and code panes (no toggle), use bare \code{\link[=ptr_ui_plot]{ptr_ui_plot()}} and
\code{\link[=ptr_ui_code]{ptr_ui_code()}} instead — a standalone \code{\link[=ptr_ui_code]{ptr_ui_code()}} is always
visible and needs no wiring.
}
\seealso{
\code{\link[=ptr_ui_plot]{ptr_ui_plot()}}, \code{\link[=ptr_ui_code]{ptr_ui_code()}}, \code{\link[=ptr_outputs_ui]{ptr_outputs_ui()}}, \code{\link[=ptr_server]{ptr_server()}}
}
