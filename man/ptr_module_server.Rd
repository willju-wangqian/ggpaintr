% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paintr-app.R
\name{ptr_module_server}
\alias{ptr_module_server}
\title{Register ggpaintr Server Logic for a Shiny Module}
\usage{
ptr_module_server(
  id,
  formula,
  envir = parent.frame(),
  ui_text = NULL,
  placeholders = NULL,
  checkbox_defaults = NULL,
  expr_check = TRUE
)
}
\arguments{
\item{id}{Module id.}

\item{formula}{A single formula string using \code{ggpaintr} placeholders.}

\item{envir}{Environment used to resolve local data objects when building the app.}

\item{ui_text}{Optional named list of copy overrides for UI labels, helper
text, and placeholders.}

\item{placeholders}{Optional custom placeholder definitions or an existing
placeholder registry.}

\item{checkbox_defaults}{Optional named list of initial checked states for
layer checkboxes. See \code{\link[=ptr_server_state]{ptr_server_state()}}.}

\item{expr_check}{Controls \code{expr} placeholder validation. See \code{\link[=ptr_app]{ptr_app()}}.}
}
\value{
A \code{ptr_state} object containing reactive accessors named \code{obj},
\code{runtime}, and \code{var_ui_list}, plus shared metadata used by the bind helpers.
}
\description{
Use this helper with \code{ptr_module_ui()} when you prefer Shiny modules for
Level 2 integration. The function also documents the module ID contract:
server-side ids stay local to the module, while UI generated from
\code{renderUI()} is namespaced with \code{session$ns}.
}
\examples{
ui <- shiny::fluidPage(ptr_module_ui("plot1"))
server <- function(input, output, session) {
  ptr_module_server(
    "plot1",
    "ggplot(data = mtcars, aes(x = var, y = var)) + geom_point()"
  )
}
if (interactive()) {
  shiny::shinyApp(ui, server)
}
}
