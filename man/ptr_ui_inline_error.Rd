% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paintr-app.R
\name{ptr_ui_inline_error}
\alias{ptr_ui_inline_error}
\title{Nest an Inline Error Slot in a Plot Piece}
\usage{
ptr_ui_inline_error(plot, error)
}
\arguments{
\item{plot}{A plot piece, typically \code{ptr_ui_plot(id)}. Must be the
\code{.ptr-card--plot} card so the error can be appended to its body.}

\item{error}{An error piece, typically \code{ptr_ui_error(id)} built with
the same \code{id} as \code{plot}.}
}
\value{
A \link[shiny:reexports]{shiny::tag} — the plot card with \code{error} nested in its body.
}
\description{
Output combinator: takes an already-built bare plot piece
(\code{\link[=ptr_ui_plot]{ptr_ui_plot()}}) and an already-built bare error piece
(\code{\link[=ptr_ui_error]{ptr_ui_error()}}) and returns the plot card with the error slot
rendered \strong{inline in the card body} — the layout the bundled apps use.
Pure DOM structure; no server coupling (the server registers
\code{ptr_plot} / \code{ptr_error} regardless). Nestable inside
\code{\link[=ptr_ui_toggle_code]{ptr_ui_toggle_code()}}.
}
\details{
This combinator does \strong{not} add the \code{.ptr-output} toggle scope (it has
no toggle, so it needs none); \code{\link[=ptr_ui_toggle_code]{ptr_ui_toggle_code()}} owns that wrapper.
}
\examples{
ptr_ui_inline_error(ptr_ui_plot("p"), ptr_ui_error("p"))
}
\seealso{
\code{\link[=ptr_ui_plot]{ptr_ui_plot()}}, \code{\link[=ptr_ui_error]{ptr_ui_error()}}, \code{\link[=ptr_ui_toggle_code]{ptr_ui_toggle_code()}}
}
