% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paintr-app.R
\name{ptr_controls_ui}
\alias{ptr_controls_ui}
\title{Control Widgets for an Embedded \code{ggpaintr} Formula}
\usage{
ptr_controls_ui(
  id = NULL,
  formula,
  ui_text = NULL,
  checkbox_defaults = NULL,
  expr_check = TRUE,
  css = NULL
)
}
\arguments{
\item{id}{Optional module id; the namespace prefix for inputs. Defaults to
\code{NULL} (identity namespace) for the single-embedding case. When set,
must match the \code{id} passed to \code{\link[=ptr_outputs_ui]{ptr_outputs_ui()}} and the
\code{shiny::moduleServer()} wrapping \code{\link[=ptr_server]{ptr_server()}} (or to
\code{\link[=ptr_module_server]{ptr_module_server()}}).}

\item{formula}{A single formula string with \code{ggpaintr} placeholders.}

\item{ui_text}{Optional named list of copy overrides; see \code{\link[=ptr_ui_text]{ptr_ui_text()}}
for the full schema and current defaults.}

\item{checkbox_defaults}{Optional named list of initial checked states.}

\item{expr_check}{Controls \code{expr} placeholder validation. Defaults to \code{TRUE}.}

\item{css}{Optional character vector of paths to additional CSS files;
linked after \code{ggpaintr}'s bundled stylesheet so its rules win. See
\code{\link[=ptr_app]{ptr_app()}} for the full semantics. Defaults to \code{NULL}.}
}
\value{
A \code{\link[shiny:reexports]{shiny::tagList()}}.
}
\description{
UI fragment containing only the generated controls (layer picker, per-layer
parameter panels, the "Update plot" button) for a \code{ggpaintr} formula. Place
it anywhere in your app's layout; pair with \code{\link[=ptr_outputs_ui]{ptr_outputs_ui()}} for the
plot/error/code panes and \code{\link[=ptr_server]{ptr_server()}} (or \code{\link[=ptr_module_server]{ptr_module_server()}}) for the
server logic. The fragment self-wraps in \verb{<div class="ptr-app">} so the
bundled stylesheet attaches without any host-side scaffolding. This is a
thin convenience composite of \code{\link[=ptr_ui_assets]{ptr_ui_assets()}} + \code{\link[=ptr_ui_controls]{ptr_ui_controls()}}.
}
\seealso{
\code{\link[=ptr_outputs_ui]{ptr_outputs_ui()}}, \code{\link[=ptr_module_ui]{ptr_module_ui()}}, \code{\link[=ptr_module_server]{ptr_module_server()}}, \code{\link[=ptr_css]{ptr_css()}}
}
