% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paintr-app.R
\name{ptr_server_state}
\alias{ptr_server_state}
\title{Build Reactive Server State for ggpaintr}
\usage{
ptr_server_state(
  formula,
  envir = parent.frame(),
  ui_text = NULL,
  ids = ptr_build_ids(),
  placeholders = NULL,
  checkbox_defaults = NULL,
  expr_check = TRUE,
  safe_to_remove = character(),
  ns = shiny::NS(NULL),
  server_ns = ns
)
}
\arguments{
\item{formula}{A single formula string using \code{ggpaintr} placeholders.}

\item{envir}{Environment used to resolve local data objects when building the app.}

\item{ui_text}{Optional named list of copy overrides for UI labels, helper
text, and placeholders.}

\item{ids}{A \code{ptr_build_ids} object describing the top-level Shiny ids used by
the integration helpers.}

\item{placeholders}{Optional custom placeholder definitions or an existing
placeholder registry.}

\item{checkbox_defaults}{Optional named list controlling the initial checked
state of each layer's "include this layer" checkbox at app launch. Names
match layer names from the formula (use \code{names(parsed$expr_list)} to
inspect; duplicate layers receive a hyphen-numeric suffix starting at
\code{-2}, e.g. a formula with two \code{geom_point()} calls produces layer names
\code{geom_point} and \code{geom_point-2}). Each value is a single logical or a
logical vector applied positionally over consecutive instances of that
layer; vectors shorter than the count of instances are padded with \code{TRUE}
and longer vectors are truncated with a warning. A deduped key wrapped in
backticks (e.g. \code{`geom_point-2`}) addresses one specific instance.
\code{NA} and non-logical values raise an error; unrecognized names raise a
warning and are ignored. Default \code{NULL} keeps every layer checked
(current behavior).}

\item{expr_check}{Controls \code{expr} placeholder validation.
\code{TRUE} (default) applies the built-in denylist of dangerous
functions.
\code{FALSE} disables all checking.
A named list with \code{deny_list} and/or \code{allow_list} character
vectors supplies a custom check; when both are given,
denied entries are removed from the allowlist.}

\item{safe_to_remove}{Character vector of additional function names whose
zero-argument calls should be dropped after placeholder substitution
leaves them empty. Extends the curated default set: \code{theme()}, \code{labs()},
\code{xlab()}, \code{ylab()}, \code{ggtitle()}, \code{facet_wrap()}, \code{facet_grid()},
\code{facet_null()}, \code{xlim()}, \code{ylim()}, \code{lims()}, \code{expand_limits()},
\code{guides()}, \code{annotate()}. User-authored zero-arg calls (where
substitution did not empty the call) and \verb{geom_*()} / \verb{stat_*()}
standalone layers are always preserved. Defaults to \code{character()}.}

\item{ns}{An optional namespace function (\code{character -> character}) used to
render UI ids and, by default, Shiny server binding ids. Pass
\code{shiny::NS("page1")} to avoid id collisions when embedding two or more
ggpaintr formulas in the same root Shiny session. Defaults to
\code{shiny::NS(NULL)} (identity — no prefixing).}

\item{server_ns}{Optional namespace function for Shiny \code{input} / \code{output}
binding keys. Defaults to \code{ns}. The module wrapper uses
\code{shiny::NS(NULL)} because \code{shiny::moduleServer()} already scopes server
bindings while rendered UI ids still need the module namespace.}
}
\value{
An object of class \code{ptr_state}.
The returned state stores \code{raw_ids} (canonical unprefixed ids),
\code{ui_ids} (rendered DOM ids), and \code{server_ids} (Shiny binding keys).
\code{ids} is retained as a compatibility alias for \code{server_ids}.
}
\description{
Create the shared reactive state used by the extensible \verb{ptr_*} server
helpers. This object can be passed to the bind helpers or inspected directly
inside a larger Shiny app.
}
\examples{
state <- ptr_server_state(
  "ggplot(data = mtcars, aes(x = var, y = var)) + geom_point()"
)
is.function(state$runtime)
}
