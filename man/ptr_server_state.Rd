% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paintr-server.R
\name{ptr_server_state}
\alias{ptr_server_state}
\title{Long-Lived Server State for a \code{ggpaintr} Formula}
\usage{
ptr_server_state(
  formula,
  envir = parent.frame(),
  ui_text = NULL,
  checkbox_defaults = NULL,
  expr_check = TRUE,
  safe_to_remove = character(),
  shared = list(),
  draw_trigger = NULL,
  producer_debounce_ms = NULL,
  ns = shiny::NS(NULL),
  server_ns = ns,
  auto_bind_shared = FALSE
)
}
\arguments{
\item{formula}{A single formula string with \code{ggpaintr} placeholders.}

\item{envir}{Environment used to resolve local data objects.}

\item{ui_text}{Optional named list of copy overrides.}

\item{checkbox_defaults}{Optional named list of initial checked states for
layer checkboxes.}

\item{expr_check}{Controls \code{expr} placeholder validation.}

\item{safe_to_remove}{Character vector of additional function names whose
zero-argument calls should be dropped after substitution.}

\item{shared}{Named list of reactives (one per shared key) supplied by an
outer wrapper such as \code{\link[=ptr_app_grid]{ptr_app_grid()}}. Defaults to \code{list()}.}

\item{draw_trigger}{Optional reactive whose invalidation forces a redraw
(e.g. the grid app's "Draw all" button).}

\item{producer_debounce_ms}{Optional. Controls the debounce window applied
to producer-style placeholder inputs (\code{text}, \code{num}, \code{expr}) before they
invalidate downstream consumer caches. \code{NULL} (default) enables auto
mode: window starts at 0 ms and the runtime flips to 300 ms after three
consecutive upstream resolutions exceed 150 ms (and back to 0 after five
consecutive resolutions under 80 ms). Pass \code{0} to force off forever, or a
positive integer to pin a manual window.}

\item{ns}{A namespace function used for rendered ids (UI side).}

\item{server_ns}{A namespace function used for server-side input lookups.
Defaults to \code{ns}.}

\item{auto_bind_shared}{If \code{TRUE}, the host (single-plot \code{ptr_app()})
auto-binds shared widgets in a top-level shared section. Relaxes the
"missing-from-bindings" check in \code{ptr_validate_shared_bindings()} and
activates the shared-\code{var} consumer gate (single-plot cannot resolve
columns for a shared widget rendered outside any layer panel).}
}
\value{
A \code{ptr_state} list (S3 class \code{c("ptr_state", "list")}).
}
\description{
Builds the \code{ptr_state} list consumed by \code{\link[=ptr_server]{ptr_server()}} and the friend
helpers (\verb{ptr_register_*}, \verb{ptr_extract_*}, \code{ptr_gg_extra}). Holds the
typed AST as a \code{reactiveVal}, the runtime result, per-pipeline-layer
resolved-data caches, the input snapshot machinery, and the shared
bindings / draw trigger when used inside \code{\link[=ptr_app_grid]{ptr_app_grid()}}.
}
