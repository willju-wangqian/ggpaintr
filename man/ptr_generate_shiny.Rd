% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paintr-export.R
\name{ptr_generate_shiny}
\alias{ptr_generate_shiny}
\title{Generate a Standalone Shiny App Script}
\usage{
ptr_generate_shiny(
  ptr_obj,
  output_file,
  ...,
  style = TRUE,
  ui_text = NULL,
  placeholders = NULL
)
}
\arguments{
\item{ptr_obj}{A \code{ptr_obj}.}

\item{output_file}{Path to the generated \code{.R} script.}

\item{...}{Backward-compatibility only. A deprecated legacy \code{var_ui} argument
may still be supplied positionally or by name, but it is ignored.}

\item{style}{Whether to style the generated file with \code{styler} when available.}

\item{ui_text}{Optional named list of copy overrides for UI labels, helper
text, and placeholders.}

\item{placeholders}{Optional custom placeholder definitions or an existing
placeholder registry. Exported custom placeholders must define their hooks
inline inside \code{ptr_define_placeholder()} so the generated app can stay
standalone.}
}
\value{
Invisibly returns \code{output_file}.
}
\description{
Generate a single-file Shiny app that uses the same \code{ggpaintr} runtime helpers
as the package.
}
\examples{
obj <- ptr_parse_formula(
  "ggplot(data = mtcars, aes(x = var, y = var)) + geom_point()"
)
out_file <- tempfile(fileext = ".R")
ptr_generate_shiny(obj, out_file, style = FALSE)
file.exists(out_file)
}
