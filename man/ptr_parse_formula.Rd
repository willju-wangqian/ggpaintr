% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paintr-parse.R
\name{ptr_parse_formula}
\alias{ptr_parse_formula}
\title{Parse a Paintr Formula}
\usage{
ptr_parse_formula(formula, placeholders = NULL, formula_check = TRUE)
}
\arguments{
\item{formula}{A single formula string describing a ggplot-like expression.}

\item{placeholders}{Optional custom placeholder definitions or an existing
placeholder registry.}

\item{formula_check}{Logical or list controlling safety validation of the
formula text itself. \code{TRUE} (default) applies the default denylist.
\code{FALSE} skips validation, treating the formula as trusted developer
input. A list with \code{deny_list} / \code{allow_list} customises the
check. See \code{validate_expr_safety} details.}
}
\value{
An object of class \code{ptr_obj}.
}
\description{
Parse a single ggplot-like formula string into a \code{ptr_obj} containing
expression metadata, placeholder locations, and generated UI definitions.
}
\details{
Supported placeholders come from the effective placeholder registry. The
built-in registry includes \code{var}, \code{text}, \code{num}, \code{expr}, and \code{upload}.
}
\note{
The \code{formula} argument is validated by default using the denylist.
Set \code{formula_check = FALSE} only for trusted developer input that you
know is safe — this is an advanced option for package authors who
programmatically generate formula strings.  This check is independent of
the per-placeholder \code{expr_check} applied at runtime; disabling one
does not disable the other.
}
\section{Column-name collisions with placeholder keywords}{

Placeholder detection is purely syntactic: any bare symbol whose name
matches a registered keyword (\code{var}, \code{text}, \code{num}, \code{expr}, \code{upload}, or
any custom keyword) is consumed as a placeholder, regardless of whether
the active dataset has a column with the same name. As a consequence, if
your data has a column literally named \code{"var"}, writing the bare symbol
\code{var} in the formula will \emph{not} reference that column — it will be
replaced by the user's \code{var} widget selection. Backtick quoting
(\code{`var`}) parses to the same R symbol and does not escape the
conflict. To reference such a column, use \code{.data[["var"]]}, which
represents the column name as a string literal (not a symbol) and is
therefore ignored by the placeholder walker. Alternatively, pick the
column from the \code{var} dropdown, which still lists every column of the
dataset (including one named \code{"var"}).
}

\examples{
obj <- ptr_parse_formula(
  "ggplot(data = mtcars, aes(x = var, y = var)) + geom_point()"
)
names(obj$expr_list)
}
