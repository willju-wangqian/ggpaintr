% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paintr-builtins.R
\name{pp_off_toggles}
\alias{pp_off_toggles}
\alias{ppLayerOff}
\alias{ppVerbOff}
\title{Off-by-default layer / pipeline-stage wrappers}
\usage{
ppLayerOff(layer_expr, hide = TRUE)

ppVerbOff(.data, verb_expr, hide = TRUE)
}
\arguments{
\item{layer_expr}{A ggplot2 layer expression (e.g. \code{geom_point()},
\code{facet_wrap(~ cyl)}). Evaluated only when \code{hide = FALSE}.}

\item{hide}{A length-1 logical literal (\code{TRUE} or \code{FALSE}). In
\code{ptr_app()} formulas this MUST be a literal — the translator aborts
on a non-literal so the formula remains the single source of truth
for the app's boot state. Defaults to \code{TRUE}.}

\item{.data}{A data frame or pipe-supplied dataset (the implicit
\code{.data} slot when used as a pipeline stage).}

\item{verb_expr}{A data-pipeline verb call (e.g. \code{mutate(mpg = mpg + 100)}, \code{filter(cyl == 6)}). Evaluated with \code{.data} inserted as the
first positional argument only when \code{hide = FALSE}.}
}
\value{
Outside \code{ptr_app()}: \code{ppLayerOff} returns \code{NULL} when
\code{hide = TRUE}, otherwise the evaluated \code{layer_expr}. \code{ppVerbOff}
returns \code{.data} unchanged when \code{hide = TRUE}, otherwise the result
of \code{verb_expr} applied to \code{.data}.
}
\description{
Two ADR-0020 \emph{structural} keywords that mark a layer or a pipeline
stage as off-by-default in \code{ptr_app()}. Inside \code{ptr_app()} /
\code{ptr_server()} the parser sees the wrapper and unwraps it at translate
time, stamping the boot-state metadata on the resulting node:
\code{ppLayerOff(layer_expr, hide = TRUE)} becomes a \code{ptr_layer} with
\code{default_active = FALSE}; \code{ppVerbOff(.data, verb_expr, hide = TRUE)}
becomes the inner verb call with \code{default_stage_enabled = FALSE}. The
wrapper itself never appears in the typed tree.
}
\details{
Outside \code{ptr_app()} they behave per their R semantics so naked-ggplot
scripts still render:
\code{ppLayerOff(geom_point(), TRUE)} returns \code{NULL} (so
\code{ggplot(mtcars, aes(x = mpg, y = wt)) + ppLayerOff(geom_point(), TRUE)}
renders without the hidden layer); \code{ppLayerOff(geom_point(), FALSE)}
returns the layer. \code{ppVerbOff(.data, mutate(x = 1), TRUE)} returns
\code{.data} unchanged; \code{ppVerbOff(.data, mutate(x = 1), FALSE)} routes
\code{.data} through the verb call.
}
\section{Data-argument position (\code{ppVerbOff} only)}{

\code{ppVerbOff(.data, verb_expr, hide = TRUE)} assumes the data must be
inserted as the \strong{first positional argument} of \code{verb_expr} when
\code{hide = FALSE}. This matches the tidyverse convention and the
translator's pipeline-stage handling; non-tidyverse verbs that take
data in a later argument are not supported via \code{ppVerbOff} (use a
lambda stage or a named wrapper instead). See
\href{../articles/adr/0020-pp-layer-off-pp-verb-off-toggles.html}{ADR 0020}
Risks §3.
}

\examples{
library(ggplot2)
# Naked-R semantics: hide = TRUE drops the layer to NULL.
p1 <- ggplot(mtcars, aes(x = mpg, y = wt)) +
  ppLayerOff(geom_point(), TRUE)        # equivalent to no geom_point
p2 <- ggplot(mtcars, aes(x = mpg, y = wt)) +
  ppLayerOff(geom_point(), FALSE)       # the layer is added

# Naked-R semantics: hide = TRUE leaves the data unchanged.
identical(ppVerbOff(mtcars, dplyr::mutate(mpg = mpg + 100), TRUE), mtcars)

# Inside ptr_app(), the wrapper becomes a node-level default and a
# boot-state-off checkbox (Plan 02 wires the UI):
# ptr_app("ggplot() + ppLayerOff(geom_point(aes(x = mpg, y = wt)), TRUE)")
}
