% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paintr-app.R
\name{ggpaintr_server_state}
\alias{ggpaintr_server_state}
\title{Build Reactive Server State for ggpaintr}
\usage{
ggpaintr_server_state(
  formula,
  envir = parent.frame(),
  copy_rules = NULL,
  ids = ggpaintr_ids(),
  placeholders = NULL
)
}
\arguments{
\item{formula}{A single formula string using \code{ggpaintr} placeholders.}

\item{envir}{Environment used to resolve local data objects when building the app.}

\item{copy_rules}{Optional named list of copy overrides for UI labels, helper
text, and placeholders.}

\item{ids}{A \code{ggpaintr_ids} object describing the top-level Shiny ids used by
the integration helpers.}

\item{placeholders}{Optional custom placeholder definitions or an existing
placeholder registry.}
}
\value{
An object of class \code{ggpaintr_state}.
}
\description{
Create the shared reactive state used by the extensible \verb{ggpaintr_*} server
helpers. This object can be passed to the bind helpers or inspected directly
inside a larger Shiny app.
}
\examples{
state <- ggpaintr_server_state(
  "ggplot(data = mtcars, aes(x = var, y = var)) + geom_point()"
)
is.function(state$runtime)
}
