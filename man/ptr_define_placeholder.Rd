% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paintr-placeholders.R
\name{ptr_define_placeholder}
\alias{ptr_define_placeholder}
\title{Construct a Custom ggpaintr Placeholder}
\usage{
ptr_define_placeholder(
  keyword,
  build_ui,
  resolve_expr,
  resolve_input = NULL,
  bind_ui = NULL,
  prepare_eval_env = NULL,
  copy_defaults = list(label = "Enter a value for {param}"),
  source_file = NULL,
  source_package = NULL,
  source_function = NULL,
  on_missing = "warn"
)
}
\arguments{
\item{keyword}{A single syntactic placeholder name used inside the formula.}

\item{build_ui}{Function with signature \verb{(id, copy, meta, context)} returning
a Shiny UI control or placeholder.}

\item{resolve_expr}{Function with signature \verb{(value, meta, context)}
returning an R expression or \code{ptr_missing_expr()}.}

\item{resolve_input}{Optional function with signature
\verb{(input, id, meta, context)} returning the raw value to hand to
\code{resolve_expr()}. Defaults to \code{input[[id]]}.}

\item{bind_ui}{Optional function with signature \verb{(input, output, metas, context)} for registering deferred UI such as the built-in \code{var}
placeholder.}

\item{prepare_eval_env}{Optional function with signature
\verb{(input, metas, eval_env, context)} returning an updated evaluation
environment.}

\item{copy_defaults}{Optional named list with \code{label}, \code{help},
\code{placeholder}, and \code{empty_text}. Defaults to
\code{list(label = "Enter a value for {param}")}.}

\item{source_file}{Character string (or named list with \code{.default} and
hook-name keys) giving the path to an R file that defines the hook
functions. The exported app will \code{source()} this file. Provide the file
alongside the exported \code{app.R}. Applies to all hooks unless overridden
per-hook with a named list.}

\item{source_package}{Character string (or named list with \code{.default} and
hook-name keys) naming an R package that exports the hook functions. The
exported app will call \code{library()} and install the package if missing.}

\item{source_function}{Named list mapping hook names to the actual function
objects (e.g. \code{list(build_ui = my_build_ui)}). The exported app will
contain the deparsed function definitions before the placeholder call.}

\item{on_missing}{One of \code{"warn"} (default) or \code{"error"}. Controls the
exported app's behaviour when a \code{source_file} file or \code{source_package}
package is unavailable at runtime.}
}
\value{
An object of class \code{ptr_define_placeholder}.
}
\description{
Build one placeholder specification for use with
\code{ptr_merge_placeholders()}. Custom placeholders can define their own
UI control, runtime expression replacement, deferred UI binding, and
evaluation-environment preparation.
}
\note{
\strong{Export limitation:} To make a custom placeholder exportable through
\code{ptr_generate_shiny()}, all hook functions (\code{build_ui}, \code{resolve_expr}, etc.)
must be defined \strong{inline} inside the \code{ptr_define_placeholder()} call.
Functions defined elsewhere and passed by name (e.g.,
\code{build_ui = my_function}) cannot be serialized into the standalone app file.
This is a known limitation; a future release may lift this restriction.
}
\examples{
date_placeholder <- ptr_define_placeholder(
  keyword = "date",
  build_ui = function(id, copy, meta, context) {
    shiny::dateInput(id, copy$label)
  },
  resolve_expr = function(value, meta, context) {
    if (is.null(value) || identical(value, "")) {
      return(ptr_missing_expr())
    }

    rlang::expr(as.Date(!!value))
  },
  copy_defaults = list(label = "Choose a date for {param}")
)
date_placeholder$keyword
}
