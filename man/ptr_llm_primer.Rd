% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paintr-llm.R
\name{ptr_llm_primer}
\alias{ptr_llm_primer}
\title{Get the ggpaintr LLM system-prompt primer}
\usage{
ptr_llm_primer()
}
\value{
A single character string.
}
\description{
Returns the text of \code{inst/llm/primer.md} as a single string. Intended
for use as the \verb{system_prompt =} (or equivalent) argument when wiring
ggpaintr into an LLM client such as \code{ellmer}, so the model knows when
to reach for ggpaintr and at which of the three integration levels.
}
\details{
The primer is short by design — it establishes the extensibility
model and points the model at \code{\link[=ptr_llm_topic]{ptr_llm_topic()}} for runnable
examples. A companion tool that exposes \code{ptr_llm_topic()} to the
model lets it pull only the example it needs, instead of loading
every topic into the system prompt.
}
\examples{
primer <- ptr_llm_primer()
cat(substr(primer, 1, 200))
}
\seealso{
\code{\link[=ptr_llm_topic]{ptr_llm_topic()}}, \code{\link[=ptr_llm_topics]{ptr_llm_topics()}}
}
