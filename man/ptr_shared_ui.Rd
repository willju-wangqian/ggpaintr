% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paintr-shared-ui.R
\name{ptr_shared_ui}
\alias{ptr_shared_ui}
\title{Render the Shared Widget Panel for a Multi-Instance Embedding}
\usage{
ptr_shared_ui(
  formulas,
  shared_ui = list(),
  ui_text = NULL,
  expr_check = TRUE,
  draw_all_label = "Draw all",
  css = NULL
)
}
\arguments{
\item{formulas}{A character vector or list of formula strings, one per
embedded \code{\link[=ptr_module_ui]{ptr_module_ui()}} instance.}

\item{shared_ui}{Named list of \verb{function(id) -> shiny.tag} builders, one
per shared key the embedder wants to customise. Unsupplied keys are
auto-rendered from the first formula that mentions them.}

\item{ui_text}{Optional copy overrides forwarded to the auto-built
widgets (see \code{\link[=ptr_app]{ptr_app()}}'s \code{ui_text} argument).}

\item{expr_check}{Whether to validate \code{expr} placeholders during
formula translation. Default \code{TRUE}.}

\item{draw_all_label}{Label for the "Draw all" button. The button is
rendered only when \code{length(formulas) >= 2}.}

\item{css}{Optional character vector of paths to additional CSS files;
linked after \code{ggpaintr}'s bundled stylesheet so its rules win. See
\code{\link[=ptr_app]{ptr_app()}} for the full semantics. Defaults to \code{NULL}.}
}
\value{
A \code{shiny.tag} wellPanel suitable for direct placement in the
embedder's UI.
}
\description{
Renders one \code{\link[shiny:wellPanel]{shiny::wellPanel()}} that hosts every shared placeholder
referenced by the supplied formulas. Drop it into a user-authored Shiny
app alongside two or more \code{\link[=ptr_module_ui]{ptr_module_ui()}} instances when those
instances need to share a \code{var}, \code{text}, \code{num}, \code{expr}, or \code{upload}
widget via \code{shared = "<key>"} annotations.
}
\details{
One \code{ptr_shared_ui()} per page. The companion \code{\link[=ptr_shared_server]{ptr_shared_server()}}
must be called in the same Shiny server function, and the resulting
\code{ptr_shared_state} threaded into each \code{\link[=ptr_module_server]{ptr_module_server()}}.

Errors when no formula in \code{formulas} declares a \code{shared = "..."}
annotation -- calling this function is a declaration of intent.
}
\seealso{
\code{\link[=ptr_shared_server]{ptr_shared_server()}}, \code{\link[=ptr_module_ui]{ptr_module_ui()}},
\code{\link[=ptr_module_server]{ptr_module_server()}}, \code{\link[=ptr_css]{ptr_css()}}.
}
