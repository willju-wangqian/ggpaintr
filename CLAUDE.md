# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What This Is

An R package that turns ggplot-like formula strings into Shiny apps.
Placeholder tokens in the formula (`var`, `text`, `num`, `expr`, `upload`) become Shiny input widgets automatically.

## Commands

```r
devtools::test()                          # run all tests
devtools::test(filter = "parse-formula")  # run one test file (matches test-<filter>.R)
devtools::document()                      # regenerate NAMESPACE + man/ from roxygen
devtools::check()                         # full R CMD check (0 errors, 0 warnings required)
devtools::load_all()                      # load package for interactive use
pkgdown::build_site()                     # build documentation site
```

## Architecture — The Pipeline

A formula string flows through four stages:

### 1. Parsing (`paintr-parse.R`)
`ggpaintr_formula()` parses the formula via rlang, splits it into layers (e.g. `ggplot()`, `geom_point()`), identifies placeholder tokens in arguments, generates unique IDs like `"ggplot+3+2"`, and returns a `ggpaintr_obj` containing ASTs, placeholder metadata, and the placeholder registry.

### 2. Placeholder Registry (`paintr-placeholders.R`)
`ggpaintr_placeholder()` defines extensible control types with four hooks: `build_ui()` (Shiny widget), `resolve_expr()` (input → R expression), `bind_ui()` (deferred/reactive UI), `prepare_eval_env()` (inject runtime data). Built-in types: `var` (column selector), `text`, `num`, `expr` (parsed R input), `upload` (CSV/RDS loader). Custom placeholders can override built-ins but must use inline function definitions (for export serialization).

### 3. UI & Runtime (`paintr-ui.R`, `paintr-runtime.R`)
- UI generation: `ggpaintr_build_ui_list()` creates Shiny controls; `var` inputs use deferred `uiOutput()` since columns depend on data.
- Expression completion: `ggpaintr_complete_expr()` substitutes user inputs into layer ASTs.
- `ggpaintr_build_runtime()` orchestrates: complete expressions → evaluate layers → build plot → validate. Returns `list(ok, plot, code_text, message, condition)`.

### 4. Shiny App & Export (`paintr-app.R`, `paintr-export.R`)
- `ggpaintr_server_state()` creates reactive state (parsed formula, runtime, deferred UI, copy rules).
- Modular binders: `ggpaintr_bind_control_panel()`, `ggpaintr_bind_draw()`, `ggpaintr_bind_plot()`, `ggpaintr_bind_export()`, `ggpaintr_bind_code()`, `ggpaintr_bind_error()`.
- `generate_shiny()` serializes a standalone `.R` app with embedded formula, custom placeholders, and copy rules.

### Supporting Modules
- **Copy rules** (`paintr-copy.R`): Nested hierarchy (shell → params/layers → keywords → fields) maps UI labels and help text with template interpolation (`{param}`, `{layer}`).
- **Upload** (`paintr-upload.R`): File metadata extraction, object-name derivation, environment injection.
- **Data** (`paintr-data.R`): Column-name sanitization for safe variable references.
- **Utils** (`paintr-utils.R`): Internal helpers.

## Conventions

- Source files: `R/paintr-*.R`. Tests: `tests/testthat/test-*.R`.
- Public API: functions prefixed `ggpaintr_` or `ggpaintr_`. Internal helpers are unexported.
- Tidyverse style: snake_case, 2-space indent.
- NAMESPACE is auto-generated by roxygen2 — never edit by hand.
- Dependencies: rlang (tidy eval + error signaling), assertthat (validation), shiny + shinyWidgets (UI).

## Source of Truth Rules

- Source code and current tests are the final authority over all notes and documentation.
- `README.Rmd` is the editable README source; `README.md` is generated output — edit `README.Rmd`, then re-knit.
- `dev/developer-notes.md` is human-maintained only — do not edit unless the user explicitly asks.
- Stored task files (`dev/tasks/*.md`) stay unchanged unless the user explicitly asks to edit them.
- Do not create ad-hoc markdown files in `.claude/knowledge/`.

## Context Loading

Read these on-demand when the task requires deeper context:

- `.claude/knowledge/project-overview.md` — durable architecture, public API surface, supported boundaries
- `.claude/knowledge/testing-strategy.md` — automated/manual test policy, fixture strategy, acceptance criteria
- `.claude/knowledge/maintenance-rules.md` — file responsibilities, maintenance workflow, archive rules
- `dev/tasks/*.md` — stored task prompts for publication workflows (read when @-mentioned)

## Prompt Contracts

When the user says **"update knowledge"**:
1. Read `.claude/knowledge/maintenance-rules.md` for file responsibilities
2. Inspect current repo state relevant to recent work
3. Update memory files (current-status, next-steps) based on what changed
4. Do not touch stable knowledge files unless their scope changed

When the user says **"clean up repo"**:
1. Run `devtools::test()`
2. Run `devtools::check(document = FALSE, manual = FALSE, args = c("--as-cran", "--no-manual"))`
3. Run `pkgdown::build_site_github_pages(new_process = FALSE, install = TRUE)`
4. Render README: `devtools::load_all("."); rmarkdown::render("README.Rmd", envir = globalenv())`
5. Report any failures before updating knowledge
6. Then follow the "update knowledge" workflow
