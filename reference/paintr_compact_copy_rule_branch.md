# Recursively Compact Copy Rules Against Defaults

Recursively Compact Copy Rules Against Defaults

## Usage

``` r
paintr_compact_copy_rule_branch(current, defaults = NULL)
```

## Arguments

- current:

  A current copy-rule branch.

- defaults:

  A default copy-rule branch.

## Value

A compacted branch or `NULL`.
