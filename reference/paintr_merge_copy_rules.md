# Recursively Merge Copy Rules

Recursively Merge Copy Rules

## Usage

``` r
paintr_merge_copy_rules(base, overrides)
```

## Arguments

- base:

  A base list.

- overrides:

  Override values.

## Value

A merged list.
