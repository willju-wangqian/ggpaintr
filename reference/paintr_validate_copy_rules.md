# Validate Copy Rules

Validate Copy Rules

## Usage

``` r
paintr_validate_copy_rules(copy_rules, placeholders = NULL)
```

## Arguments

- copy_rules:

  User-supplied copy rules.

- placeholders:

  Optional custom placeholder definitions or an effective placeholder
  registry.

## Value

Invisibly returns `TRUE`.
