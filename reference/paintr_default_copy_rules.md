# Default Copy Rules for ggpaintr

Internal registry for all user-facing control copy. Maintainers should
update this object when they want to refine defaults or add support for
new common parameters in one place.

## Usage

``` r
paintr_default_copy_rules()
```

## Value

A named list of default copy rules.
