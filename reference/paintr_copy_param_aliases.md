# Normalize Parameter Aliases for Copy Rules

Add new aliases here when different parameter spellings should resolve
to the same copy entry.

## Usage

``` r
paintr_copy_param_aliases()
```

## Value

A named character vector mapping aliases to canonical keys.
