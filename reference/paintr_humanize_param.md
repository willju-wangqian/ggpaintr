# Build a Human-Readable Parameter Label

Build a Human-Readable Parameter Label

## Usage

``` r
paintr_humanize_param(param)
```

## Arguments

- param:

  A parameter name or `NULL`.

## Value

A single readable label string.
