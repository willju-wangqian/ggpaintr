# Resolve Standard Shell Copy for ggpaintr

Resolve Standard Shell Copy for ggpaintr

## Usage

``` r
paintr_resolve_shell_copy(copy_rules = NULL)
```

## Arguments

- copy_rules:

  Optional named list of copy overrides.

## Value

A named list with shell copy entries.
