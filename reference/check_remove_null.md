# Drop `NULL` Elements from a List

Drop `NULL` Elements from a List

## Usage

``` r
check_remove_null(x)
```

## Arguments

- x:

  A list or `NULL`.

## Value

A list without `NULL` values, or `NULL`.
