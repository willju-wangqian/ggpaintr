# Replace Copy Template Tokens

Replace Copy Template Tokens

## Usage

``` r
paintr_interpolate_copy_text(text, param = NULL, layer_name = NULL)
```

## Arguments

- text:

  A text template.

- param:

  A parameter label.

- layer_name:

  A layer name.

## Value

A single string or `NULL`.
