# Normalize User Copy Rule Keys

Normalize User Copy Rule Keys

## Usage

``` r
paintr_normalize_copy_rules(copy_rules)
```

## Arguments

- copy_rules:

  User-supplied copy rules.

## Value

A normalized copy-rule list.
